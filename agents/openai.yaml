interface:
  display_name: "Codex Luna Delegation"
  short_description: "Configure and safely orchestrate bounded Sol/Luna work"
  default_prompt: "Set up or use a bounded Luna custom agent. Preserve unrelated configuration, apply a context-isolation decision gate, define success and surface boundaries, require acceptance evidence, validate changes, and explain the credit/speed tradeoff."
policy:
  allow_implicit_invocation: true
